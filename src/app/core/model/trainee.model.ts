export interface Trainee {
    employeeCode? : string;        // EmployeeCode is the primary key
    name?: string;               // Optional property
    email?: string;              // Optional property
    batch?: string;              // Optional property
    isActive?:boolean;
  }
  